name: lint-test

on:
  push:

jobs:
  run:
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version: "1.27.0"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9
        with:
          version: latest

      - name: check YML
        run: yq . .traefik.yml

      - name: check Traefik module path
        run: |
          module_path="$(go list -m)"
          test "$module_path" = "github.com/libops/captcha-protect"
          test "$(yq '.import' .traefik.yml)" = "$module_path"

      - name: go mod tidy
        run: |
          go mod tidy
          if ! git diff --exit-code go.mod go.sum; then
            echo "Error: go.mod or go.sum is not tidy. Run 'go mod tidy' locally and commit the changes."
            exit 1
          fi

      - name: go mod vendor
        run: |
          go mod vendor
          if ! git diff --exit-code vendor/; then
            echo "Error: vendor/ directory is not up-to-date. Run 'go mod vendor' locally and commit the changes."
            exit 1
          fi

      - name: unit test
        run: go test -v -skip 'TestStateOperationsWithinThreshold' -race ./...

      - name: generate coverage
        run: go test -skip 'TestStateOperationsWithinThreshold' -coverprofile=coverage.out -covermode=atomic $(go list ./... | grep -v '/ci') || echo continue

      - name: upload coverage to codecov
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7
        with:
          files: ./coverage.out
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  integration-test-labels:
    needs: [run]
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        traefik: [v3.6, v3.7, latest]
    env:
      TRAEFIK_TAG: ${{ matrix.traefik }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: run
        run: go run test.go
        working-directory: ./ci

      - name: capture logs
        if: ${{ always() }}
        run: docker compose logs --tail 100 nginx nginx2 traefik
        working-directory: ./ci

      - name: cleanup
        if: ${{ always() }}
        run: docker compose down
        working-directory: ./ci

  integration-test-multilayer-routing:
    needs: [run]
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        traefik: [v3.6, v3.7, latest]
    env:
      COMPOSE_FILE: docker-compose.multilayer.yml
      TRAEFIK_TAG: ${{ matrix.traefik }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: run
        run: go run test.go
        working-directory: ./ci

      - name: capture logs
        if: ${{ always() }}
        run: docker compose logs --tail 100 nginx nginx2 traefik
        working-directory: ./ci

      - name: cleanup
        if: ${{ always() }}
        run: docker compose down
        working-directory: ./ci

  integration-test-cap:
    needs: [run]
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        traefik: [v3.6, latest]
    env:
      TRAEFIK_TAG: ${{ matrix.traefik }}
      CAP_E2E_PORT: "80"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: run
        run: go run ./cap
        working-directory: ./ci

      - name: capture logs
        if: ${{ always() }}
        run: docker compose -f docker-compose.cap.yml logs --tail 200 cap traefik
        working-directory: ./ci

      - name: cleanup
        if: ${{ always() }}
        run: docker compose -f docker-compose.cap.yml down -v
        working-directory: ./ci
